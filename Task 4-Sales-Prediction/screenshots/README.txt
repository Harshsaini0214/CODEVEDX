After running the app (streamlit run app.py), take screenshots of:
- Home page          -> save as home.png
- Dashboard          -> save as dashboard.png
- Prediction page    -> save as prediction.png
- EDA / Analytics    -> save as analytics.png

These are referenced in the main README.md's "Dashboard Preview" section.
